@model Do_An_Chuyen_Nganh.Models.Warranty

@{
    ViewData["Title"] = "Edit";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h1>Edit</h1>

<h4>Warranty</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            @Html.HiddenFor(model => model.Id)
            @*            <input type="hidden" asp-for="Id" name="Id" /> *@
            <div class="form-group">
                <label asp-for="WarrantyName" class="control-label">Tên Màu</label>
                <input name="WarrantyName" class="form-control" value="@Html.DisplayFor(item => item.WarrantyName)" />
                <span asp-validation-for="WarrantyName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
        <div class="d-flex">
            <a class="btn btn-back" href="/Admin/ManageWarranty/Index">
                Về danh sách
            </a>
        </div>
    </div>
</div>

